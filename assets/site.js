const buttons=[...document.querySelectorAll('.filter')],cards=[...document.querySelectorAll('.card')],search=document.querySelector('#search');function go(){const a=document.querySelector('.filter.active')?.dataset.filter||'All',q=(search?.value||'').toLowerCase();cards.forEach(c=>c.style.display=((a==='All'||(c.dataset.category||'').split('|').includes(a))&&c.innerText.toLowerCase().includes(q))?'flex':'none')}buttons.forEach(b=>b.onclick=()=>{buttons.forEach(x=>x.classList.remove('active'));b.classList.add('active');go()});search?.addEventListener('input',go);